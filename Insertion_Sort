# Insertion Sort

def insertion_sort (arr):
	for k in range (1,len(arr)):
		currNum = arr[k]
		j = k

		while j > 0 and arr[j-1] > currNum:
			arr[j] = arr[j-1]
			j -= 1
			arr[j] = currNum
		print (arr)
		
target_array = [12,7,4,13,8,43,21]

print (insertion_sort(target_array))
