# Sum of first n numbers of a sequence

import sys

target_array = [4,3,6,2,8]

def linear_sum(S, n):
 	#Return the sum of the first n numbers of sequence S.
 	if n == 0:
 		return 0
 	else:
 		return linear_sum (S,n-1) + S[n-1]

print (linear_sum (target_array,5))
